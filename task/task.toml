artifacts = ["/app/report.json"]

[task]
name = "dynamo/document-collection-reporting"
description = "Aggregate and report metrics from multi-collection JSON documents under /app/data to /app/report.json."

[metadata]
# --- Pre-seeded for you when this repo was created – DO NOT EDIT ---
category = "Data Querying and Databases"
subcategory = "NoSQL and document stores"
# --- Fixed for this dataset – LEAVE AS-IS ---
model_tested = "Opus-4.8"
agent_tested = "Terminus-2"
# --- You fill these in ---
task_objective = ["implement"]
artifact_type = ["data_processing_script"]
expert_time_estimate_hours = 8.0
difficulty_explanation = "Requires handling document schema variations, multi-key normalization, case-insensitive ID matching, tie-breaking rules, and exact floating-point rounding across multi-collection datasets."
solution_explanation = "Parse input document collections under /app/data, apply status filtering and casing normalization, aggregate net spend and quantities, apply tie-breaking logic, and format the output as JSON."
verification_explanation = "Tests check that /app/report.json exists, adheres strictly to the defined schema with no extra fields, and contains exact calculated values for all top-level metrics."

[verifier]
timeout_sec = 120.0

[agent]
timeout_sec = 120.0